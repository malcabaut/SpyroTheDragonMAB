<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/guide_01"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_generico"
    android:clickable="true"
    android:focusable="true"
    android:visibility="gone"
    tools:visibility="visible"
    tools:context=".MainActivity">

    <!-- Guía para centrar elementos en la pantalla -->
    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/guideline_center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintGuide_percent="0.5"
        android:orientation="vertical" />

    <!-- Título -->
    <TextView
        style="@style/H1"
        android:id="@+id/guide_intro_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/guide_intro_title"
        android:textColor="#FFFFFF"
        android:textSize="26sp"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="32dp"
        tools:text="Bienvenido a la Guía" />

    <!-- Texto introductorio -->
    <TextView
        style="@style/description"
        android:id="@+id/guide_intro_sub"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/guide_intro_sub"
        android:gravity="center"
        android:paddingTop="12dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/guide_intro_title"
        android:layout_marginHorizontal="24dp"
        tools:text="Esta es una guía interactiva para ayudarte a entender la aplicación." />

    <!-- Imagen principal -->
    <ImageView
        android:id="@+id/litter_spyro"
        android:contentDescription="@string/logo_spyro_image"
        android:layout_width="160dp"
        android:layout_height="160dp"
        android:src="@drawable/spyro"
        app:layout_constraintTop_toBottomOf="@id/guide_intro_sub"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"
        tools:src="@drawable/spyro" />

    <!-- Logo debajo de la imagen -->
    <ImageView
        android:id="@+id/spyro_logo_text"
        android:contentDescription="@string/down_logo_spyro_text"
        android:layout_width="160dp"
        android:layout_height="100dp"
        android:src="@drawable/splash"
        app:layout_constraintTop_toBottomOf="@id/litter_spyro"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="-40dp"
        tools:src="@drawable/splash" />

    <!-- Segundo párrafo -->
    <TextView
        android:id="@+id/spyro_logo_text_s"
        style="@style/description"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/guide_intro_p"
        android:gravity="center"
        android:paddingTop="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/spyro_logo_text"
        android:layout_marginHorizontal="24dp"
        tools:text="Sigue los pasos para completar la guía de inicio." />

    <!-- Botón "Comenzar guía" -->
    <Button
        android:id="@+id/btnNext"
        style="@style/ButtonStartGuide"
        android:text="@string/start_guide"
        app:layout_constraintTop_toBottomOf="@id/spyro_logo_text_s"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="12dp"
        tools:text="Comenzar" />

    <!-- Botón "Saltar guía" -->
    <Button
        android:id="@+id/btnSkip"
        style="@style/ButtonStartGuide"
        android:text="@string/skip_guide"
        app:layout_constraintTop_toBottomOf="@id/btnNext"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp"
        tools:text="Saltar" />

</androidx.constraintlayout.widget.ConstraintLayout>
